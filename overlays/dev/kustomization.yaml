apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../bgd-base
namePrefix: dev-
commonLabels:
  environment: dev

patches:
  - patch: |-
      - op: replace
        path: /spec/template/spec/containers/0/env/0/value
        value: yellow
    target:
      group: apps
      version: v1
      kind: Deployment
      name: bgd
